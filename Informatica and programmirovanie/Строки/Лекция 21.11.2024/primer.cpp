# include <iostream>
# include <string>
int main(){
    int n;
    std::cin >> n;
    std::cin.get();
    std::string str;
    getline(std::cin, str);
    std::string::size_type k, pos;
    str.length();
    str.size();
    std::cout << str << "\n";
    return 0;
}