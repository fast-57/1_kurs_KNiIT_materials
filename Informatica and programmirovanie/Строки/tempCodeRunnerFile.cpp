     // string s2 = str.substr(p2 + 1, str.size() - p2 - 2); // строка даты смерти
        // int d1 = stoi(s1.substr(0,2)), d2 = stoi(s2.substr(0,2)); // перевод строки в числа
        // int m1 = stoi(s1.substr(3,2)), m2 = stoi(s2.substr(3,2));
        // int y1 = stoi(s1.substr(6,4)), y2 = stoi(s2.substr(6,4));
        // if ((y2 - y1 > 50 || (y2 - y1 == 50 && m2 >= m1 && d2 >= d1)) && (1800 < y1 && y1 < 1901 || 1800 < y2 && y2 < 1901)) {
        //     out << str << '\n'; // вывод в файл
        // }